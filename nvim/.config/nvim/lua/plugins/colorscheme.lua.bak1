-- Replace your current colorscheme config with this:
return {
  {
    name = "custom-colorscheme",
    dir = vim.fn.stdpath("config"), -- Points to your nvim config directory
    lazy = false,
    priority = 1000,
    config = function()
      -- Load your custom colorscheme
      vim.cmd("colorscheme custom")
    end,
  },
}
