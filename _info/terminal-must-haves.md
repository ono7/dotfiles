# terminal programs that are awesome

`age` - encryption
`termshark` - uses tshark and runs on terminal, has TUI that looks similar to wireshare
`sshs` - ssh connection manager written in go uses ~/.ssh/config
`pass` - password manager
