# creating layers

- Automatic layer creation can be done by selecting objects/subjects/backgrounds etc then clicking on the adjustment tool
  this will automatically create a layer and mask

      1. select subject
      2. select the adjustment tool
