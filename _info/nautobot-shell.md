# Nuatobot shell commands

`invoke nbshell`

## 1. Delete all IP Addresses

IPAddress.objects.all().delete()

## 2. Delete all Prefixes

Prefix.objects.all().delete()

## 3. Delete all devices

Device.objects.all().delete()
